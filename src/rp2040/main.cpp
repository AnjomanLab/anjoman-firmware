#include <Arduino.h>
#include "MotorDriver.h"
#include "MagneticEncoder.h"

// This file is in the feat/motor-driver branch as a placeholder
// to preserve the project structure. The test and calibration codes have been moved to the
// examples/ folder.


void setup() {
    Serial.begin(115200);
    // To use the calibration codes, copy the contents of the corresponding file in the examples folder here.
}

void loop() {
    delay(1000);
}
